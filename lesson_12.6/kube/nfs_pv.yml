apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  nfs:
    # path: /mnt/nfs_share/kubedata
    path: /tmp/nfs
    server: <ip address>
  persistentVolumeReclaimPolicy: Retain
